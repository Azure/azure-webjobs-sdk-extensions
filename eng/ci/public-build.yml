# This build is used for public PR and CI builds.

trigger:
  batch: true
  branches:
    include:
    - dev
    - release/*
  tags:
    include:
    - v*
    - '*-v*'

schedules:
# Ensure we build nightly to catch any new CVEs and report SDL often.
- cron: "0 3 * * *"
  displayName: Nightly Build
  branches:
    include:
    - dev
  always: true

pr:
  branches:
    include:
    - dev
    - release/*

resources:
  repositories:
  - repository: 1es
    type: git
    name: 1ESPipelineTemplates/1ESPipelineTemplates
    ref: refs/tags/release

extends:
  template: v1/1ES.Unofficial.PipelineTemplate.yml@1es
  parameters:
    pool:
      name: 1es-pool-azfunc-public
      image: 1es-windows-2022
      os: windows
      ${{ if eq( variables['Build.Reason'], 'Schedule' ) }}:
        demands:
        - Priority -equals Low

    sdl:
      codeql:
        compiled:
          enabled: true
        runSourceLanguagesInSourceAnalysis: true

    settings:
      # PR's from forks do not have sufficient permissions to set tags.
      skipBuildTagsForGitHubPullRequests: ${{ variables['System.PullRequest.IsFork'] }}

    stages:
    - stage: Build
      jobs:
      - template: /eng/ci/templates/jobs/build.yml@self
        parameters:
          sign: false
          projects: $(Build.SourcesDirectory)/WebJobs.Extensions.sln

      - template: /eng/ci/templates/jobs/build.yml@self
        parameters:
          sign: false
          projects: $(Build.SourcesDirectory)/eng/sln/WebJobs.Extensions.Preview.proj
          label: preview
